{
  "name": "imob-monorepo",
  "version": "1.0.0",
  "description": "Monorepo: site publico + painel de imoveis/leads + backend de chat com Claude, deploy unico no Railway",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.85.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.0"
  }
}
